const backtestFieldsTooltips = {
  TICK_INTERVAL_MINUTES:
    'The interval in minutes between each tick of the backtest. Small intervals provide more inference points, but also increase the backtest analysis time. Depending on your strategy, you may not need to use the smallest interval available.',
};

export default backtestFieldsTooltips;
